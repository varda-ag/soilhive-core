{
  "name": "soilhive-project",
  "private": true,
  "description": "",
  "version": "0.2.0",
  "scripts": {
    "dev": "pnpm --filter=!common --parallel dev",
    "build": "pnpm --filter=!common --parallel build"
  }
}
