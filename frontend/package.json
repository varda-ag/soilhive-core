{
  "name": "soilhive-frontend-project",
  "private": true,
  "description": "",
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter=!common --parallel dev",
    "build": "pnpm --filter=!common --parallel build"
  },
  "dependencies": {
    "h3-js": "^4.3.0"
  }
}
