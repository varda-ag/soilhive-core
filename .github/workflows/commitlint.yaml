name: Commitlint

on:
  pull_request:
    types: 
      - 'opened'
      - 'edited'
      - 'reopened'
      - 'synchronize'
    branches:
      - main

jobs:

  commitlint:
    uses: varda-ag/.github/.github/workflows/commitlint.yaml@dfbf503b1c958885bb1e71732936cc4a1d25739b # v0.2.1
    with:
      pr_base_sha: ${{ github.event.pull_request.base.sha }}
      pr_head_sha: ${{ github.event.pull_request.head.sha }}
      pr_title: ${{ github.event.pull_request.title }}
    permissions:
      contents: read
      pull-requests: write